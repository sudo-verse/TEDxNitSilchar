// Auth-related types have been removed as login functionality is no longer available

// Basic user type kept for compatibility if needed
export interface BasicUser {
    id: string;
    name?: string;
    email: string;
}
